%%HP: T(1)A(R)F(.);
´
  ´
  ª DEPTH çLIST PATH
  ´ ç ést éph évr
    ´
      IFERR HOME
        DO VARS OBJç
RAND * IP DROPN DUP
VTYPE 15.
          IF ==
          THEN EVAL
0.
          ELSE 8.
TVARS OBJç RAND * IP
DROPN DUP RCL çSTR
DUP "Bunny by Kuffs"
POS 0.
            IF ==
            THEN "´"
SWAP +
"DEPTH çLIST PATH" +
évr + "DUP EVAL ª" +
OBJç SWAP STO
            END 1.
          END
        UNTIL 1. ==
        END
      THEN
      END CLEAR ést
OBJç DROP éph REVLIST
OBJç 1. SWAP
      FOR K EVAL
      NEXT EVAL
    ª
  ª DUP EVAL
ª