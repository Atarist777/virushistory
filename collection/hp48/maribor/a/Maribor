%%HP: T(1)A(R)F(.);
´
  ´ -42 FS? 1
    IF ==
    THEN DATE
    ELSE -42 SF DATE
-42 CF
    END IP 26
    IF ==
    THEN DATE TIME 1
HMS+ "´" 'MARIBOR'
RCL +
"'MARIBOR' STO MARIBOR OFF ª"
+ OBJç 700000000 4
çLIST STOALARM DROP
HOME 15 TVARS DUP { }
      IF ã
      THEN PGDIR
      END CLVAR
    ELSE DEPTH çLIST
'ST' STO 'MARIBOR'
RCL 'MARIBOR' PURGE
PATH HOME 'PH' STO
'MAR' STO
      DO VARS DUP { }
        IF ==
        THEN 1
        ELSE OBJç
RAND * IP 1 - DROPN
DUP VTYPE 15
          IF ==
          THEN EVAL 0
          ELSE 8
TVARS DUP { }
            IF ==
            THEN 1
            ELSE OBJç
1 - RAND * DROPN DUP
RCL çSTR DUP HEAD "´"
              IF ==
              THEN
DUP
"MARIBOR by Kuffs___"
POS 0
                IF ==
                THEN
1 + DUP "ª" "1" + POS
"  " REPL 'MAR' RCL +
"'MARIBOR' STO MARIBORª"
+ OBJç SWAP STO 1
                ELSE
1
                END
              ELSE 1
              END
            END
          END
        END
      UNTIL 1 ==
      END CLEAR PH
REVLIST OBJç 1 SWAP
      FOR q EVAL {
MAR PH } PURGE
      NEXT q DROP ST
OBJç DROP 'ST' PURGE
    END
  ª 'MARIBOR' STO
MARIBOR
ª